{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "postgres-cluster.namespace" . }}
  labels:
    {{- include "postgres-cluster.labels" . | nindent 4 }}
{{- end }}
